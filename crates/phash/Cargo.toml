[package]
name = "phash"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
ffmpeg-next = "8.1.0"
image = { version = "0.25", default-features = false }
image_hasher = "3.0"
log = "0.4"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
